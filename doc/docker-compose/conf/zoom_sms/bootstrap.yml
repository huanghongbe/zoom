server:
  port: 8604

spring:
  application:
    name: zoom-sms
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: nacos:8848
        namespace: prod
      config:
        server-addr: nacos:8848
        file-extension: yaml
        #指定分组
        group: prod
        #指定命名空间
        namespace: prod
