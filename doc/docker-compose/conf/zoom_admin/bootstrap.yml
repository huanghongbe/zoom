server:
  port: 8601
spring:
  application:
    name: zoom-admin
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: nacos:8848
        namespace: prod
      config:
        server-addr: nacos:8848
        #文件后缀
        file-extension: yaml
        #指定分组
        group: prod
        #指定命名空间
        namespace: prod